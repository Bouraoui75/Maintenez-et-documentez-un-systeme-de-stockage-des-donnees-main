# --- Mongo root (utilisé par l'entrypoint Mongo au tout premier démarrage) ---
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=changeme

# --- Cible du projet ---
MONGO_DB=healthcare
MONGO_COLLECTION=patients
MONGO_HOST=mongodb
MONGO_PORT=27017

# --- Utilisateur applicatif (déjà en place) ---
APP_USER=appuser
APP_PASSWORD=changeme

# --- Nouveaux utilisateurs (pour init-mongo.js) ---
READONLY_USER=readOnlyUser
READONLY_PASSWORD=changeme

WRITE_USER=writeUser
WRITE_PASSWORD=changeme

SUPPORT_USER=supportUser
SUPPORT_PASSWORD=changeme

ADMIN_USER=adminUser
ADMIN_PASSWORD=changeme

# --- Données à importer ---
CSV_PATH=/data/healthcare_dataset.csv
